services:
  hwpx-api:
    build: .
    container_name: hwpx-report-api
    ports:
      - "5001:5001"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./temp_outputs:/app/temp_outputs
    restart: unless-stopped
